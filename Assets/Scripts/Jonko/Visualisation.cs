using UnityEngine;

namespace Jonko.Visualisation
{
    public class Visualisation 
    {
        public static TextMesh CreateWorldText(string text, Transform parent = null, Vector3 localPosition = default(Vector3), int fontSize = 40, Color color)
    }
}
